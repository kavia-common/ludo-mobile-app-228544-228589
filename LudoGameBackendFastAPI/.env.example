# --- Database / cache ---
POSTGRES_URL=
REDIS_URL=

# --- Auth ---
# Required for JWT issuance/validation (HS256).
JWT_SECRET=
# Optional issuer/audience checks
JWT_ISSUER=
JWT_AUDIENCE=
JWT_EXP_SECONDS=3600
